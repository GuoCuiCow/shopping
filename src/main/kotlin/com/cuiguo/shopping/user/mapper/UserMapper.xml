<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace属性是名称空间，必须唯一 -->
<mapper namespace="com.cuiguo.shopping.mapper.UserMapper">
    <!-- resultMap标签:映射实体与表
         type属性：表示实体全路径名
         id属性：为实体与表的映射取一个任意的唯一的名字
    -->
    <resultMap id="UserMapper" type="com.cuiguo.shopping.model.User">
        <!-- id标签:映射主键属性
     result标签：映射非主键属性
     property属性:实体的属性名
     column属性：表的字段名
-->
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="name" column="name"/>
    </resultMap>

    <!-- 用户信息 -->
    <select id="detail" resultMap="UserMapper">
      select phone, name, create_time, update_time from t_user where id = #{id}
    </select>

    <!-- 删除产品 -->
    <delete id="delete">
        delete from t_user where id = #{id}
    </delete>
</mapper>
